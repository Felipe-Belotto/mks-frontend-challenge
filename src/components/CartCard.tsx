import React from 'react'

interface CartCardProps {
  name: string
  quantity: number
  price: number
}

export default function CartCard(props: CartCardProps) { 
  return (
    <div>
      
    </div>
  )
}
